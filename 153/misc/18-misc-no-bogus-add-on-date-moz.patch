# HG changeset patch
# User Betterbird <betterbird@betterbird.eu>
# Date 1778702455 -7200
# Parent  74cb7a1306c08054762009fc2fde1753540919c5
Misc: Don't show add-on 'last updated' dates before 2000.

Fix for https://github.com/flathub/eu.betterbird.Betterbird/issues/126. The FlatPak language packs are distribution extensions
which for some reason have a 'last updated' date of 01 Jan 1970. Anything before the year 2000 is impossible anyway.

diff --git a/toolkit/mozapps/extensions/content/components/addon-details.mjs b/toolkit/mozapps/extensions/content/components/addon-details.mjs
--- a/toolkit/mozapps/extensions/content/components/addon-details.mjs
+++ b/toolkit/mozapps/extensions/content/components/addon-details.mjs
@@ -554,9 +554,9 @@ export class AddonDetails extends AboutA
     }
 
     // Last updated.
     let updateDate = this.querySelector(".addon-detail-row-lastUpdated");
-    if (addon.updateDate) {
+    if (addon.updateDate && addon.updateDate.getFullYear() >= 2000) {
       let lastUpdated = addon.updateDate.toLocaleDateString(undefined, {
         year: "numeric",
         month: "long",
         day: "numeric",
